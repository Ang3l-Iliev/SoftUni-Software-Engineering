function firstAndLastElements(input) {
    let firstElements = input[0]
    let lastElements = input[input.length-1]
    console.log(firstElements + lastElements);
    
}

firstAndLastElements([20, 30, 40])